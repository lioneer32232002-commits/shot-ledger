# HANDOFF 2026-07-14（單一交接入口，取代 HANDOFF_20260713.md）

## 現況（全部已上線）

- **線上**：https://shot-ledger.pages.dev（Cloudflare Pages）
- **GitHub**：https://github.com/lioneer32232002-commits/shot-ledger（公開）
- **版本**：M5.1。可安裝 PWA，SW 快取版號 **v8**（改版必遞增）。
- **測試**：`node tools/test_stats.mjs` → 83 項全過（CI 部署前也會跑）。

## 部署程序（2026-07-13 起全自動）

**push 到 main 即自動部署**：GitHub Actions（`.github/workflows/deploy.yml`）
先跑 test_stats（沒全過不部署）再 wrangler 部署。
- Secrets：`CLOUDFLARE_API_TOKEN`（僅 Pages Edit 權限）＋`CLOUDFLARE_ACCOUNT_ID`。
- 直傳專案不能改接 CF Git 整合（官方限制），Actions 是等效方案。
- 狀態看 repo Actions 分頁。備用手動指令見舊交接（git archive + wrangler pages deploy）。

## 本輪完成（M4 → M4.4，使用者實地用後五輪回饋全消化）

- **M4**：最後一輪不再自動結算（核心 bug：completeRound 直接 finishSession，
  使用者誤觸就被結算＋挑戰因時長不足判失敗）→ 完成面板＋「結束並結算」；
  輪次標示進輸入區；快速/逐球一行說明；選中點光暈；底角三分歸線
  （cx 45/705）；熱區顯示命中率；成績卡照片背景（暗化壓字）。
- **M4.1**：罰球 cy 290（壓線）／罰球線頂中距 cy 350 分離；熱區大點＋
  點擊資訊列（court.js 內部自理，renderCourt 介面不變）；分享卡放大。
- **M4.2**：選項按鈕統一膠囊 segmented（.option-segmented）；SHOT 鈕
  籃球條紋；動作大鈕膠囊＋ring；球場點名 26px；照片模式亮橘 #FF8A3D＋
  徽章實底；分享卡球種欄對齊。
- **M4.3**：數字圓鈕列式排版末列置中（aspect-ratio 鎖正圓）；熱區 %
  34px 貫穿圓點；分享卡 drawCard 改顯式游標推進（TextMetrics 實高）
  修「投中」與球種列重疊。
- **M4.4**：分享卡上帶左右兩欄（左 57% 大字＋投中、右球種直排跨行
  切齊）；sheet 按鈕層級（全寬主鈕 ≤1）；卡上 % 同 App 比例 4.5%；
  **描邊全部移除**（使用者指定嘗試）；修 `[hidden]` 被 .btn display
  蓋掉的全域 bug。
- 規格全在 docs/SPEC_M4.md ~ SPEC_M4_4.md。

## M5（2026-07-14，使用者三需求一次上）

- **階梯 6→12 關（球員生涯階段制）**：Lin 拆 哈佛→發展聯盟→NBA 三關；Dirk／
  Allen／Klay／Curry 各插一個前期階段關。既有 6 菜單 id/passRule/輪次一字不動
  （只改 tier），舊進度靠 store schema v5 遷移補解鎖低階關（**祖父條款**：舊
  用戶已過的關照樣顯示已通過、目前關卡不變，新插關視同已通過可自由重打）。
  階段 career 數據 2026-07-14 雙來源查證（紀錄在 menus.js 檔頭註解）；career
  新增選填 label 欄（caption 用，如「哈佛 2006–10」）。ladder_complete 徽章
  與「第 X 關 / N」都改動態取階梯長度。
- **每輪達標預估（stats.challengeForecast，8 個新測試）**：rule bar 下顯示
  「還需 X 球（剩 Y 球額度）」／「已達標 ✓」／「已無法達標」；輸入區 chip
  「本輪至少 N 球」；數學上不可達標時彈低調橫幅，兩鈕「繼續投完」（收合，
  本節不再彈）／「提前結束並結算」（走 finishSession，**紀錄全保留計入統計**，
  與整節刪除的「放棄」不同）。未來輪以每輪 10 球估算。
- **分享卡預設底圖**：assets/cardbg/bg1~bg5.jpg（Unsplash License，1080×1350
  q80 共約 790KB，Fable 用 System.Drawing 預裁）。預設 settings.cardBg='bg1'
  （最暗、白字對比最佳）；sheet 內水平 tile 選擇列（紙感／bg1~5／自訂照片）
  取代舊「用自己的照片」兩顆鈕；選紙感/bgN 會持久化、自訂照片仍只活在記憶體。
  SW CORE 加 5 張圖、CACHE_NAME v6→v7。
- 規格：docs/SPEC_M5.md。驗收已跑：83 測試、菜單完整性 node 檢核、v4→v5 遷移
  （舊/新用戶）、預估三態＋橫幅兩路徑、分享卡切換/持久化/重開記憶（取渲染
  像素驗證）、深淺色 token、觸控 48px。

## M5.1（2026-07-14，M5 上線後三則回饋）

- **六個新關菜單動線差異化**（被抓到 Klay 新秀≈Allen 雄鹿只差順序）：每關給
  結構性 motif——哈佛=禁區雙連發課表、發展聯盟=左右翼折返、Dirk 新秀=罰球線
  頂單點鑽研、Allen 雄鹿=底角雙連發、Klay 新秀=同點連兩輪、Curry MVP=近→遠
  爬坡。**既有 6 關序列是報導考據、永遠不動**。Lillard 經使用者確認保留
  （深三訓練考據最強：4 分線特訓）。
- **「節」→「練習」**：所有使用者可見文案改完（練習詳情／刪除這次練習／
  N 次練習…），程式內部命名與註解不改。驗收用 body.innerText 掃「節」字。
- **統計頁頂部生涯累計卡**（.lifetime-card）：總投/總中/命中率大字＋
  「N 次練習・M 輪」，回答「總球數去哪看」。
- 規格：docs/SPEC_M5_1.md。SW v7→v8。
- ⚠️ 本輪驗收再次踩到快取三層陷阱（本機測試中 SW 會被 app 重新註冊回來，
  驗收中途改檔後**必須重跑完整清理流程**，不是開場清一次就好）。

## M6（2026-07-14，使用者回報「一進站就是 #/train，沒有首頁」）

- **首頁 landing（#/home，js/home.js）**：三段全屏照片介紹 ＋ 一屏三入口
  （挑戰階梯／自由練習／綜合巡迴）。**訪客登陸頁**定位：裸網址且
  `settings.homeSeen === false` 才落地首頁，按任一入口即寫 true，之後裸網址
  直接進 #/train；`#/home` 永遠手動回得去（設定頁新增「重看首頁介紹」）。
  首頁不進 tab bar（`body.is-home` 藏 tab bar、清 `.app-main` padding）。
- **store schema v6→v7**：新增 `settings.homeSeen`（v6 已被同日 M5.2 占用，順延 v7）。**祖父條款**：遷移時
  「有 sessions 或已解鎖第一關以外的關」一律設 true，舊用戶不會被介紹頁擋。
- **質感三支柱**（使用者明令「不要只是把字推上照片」）：視差（圖 0.25 係數慢速
  位移，一個 scroll listener 一次算完三段）／scrim 逐圖配方向性漸層（統一壓黑
  會把照片壓髒）／進場序號→標題→內文錯開 60ms。三者在
  `prefers-reduced-motion` 下全關（連 listener 都不掛）。
- 照片：`assets/home/home1~3.jpg`（Unsplash，center-crop 3:4 → 1080×1440 q76，
  共 573KB，PowerShell + System.Drawing 預裁，同 M5 cardbg 手法）。
- SW v9→v10，CORE 加 js/home.js ＋三張圖。規格：docs/SPEC_M6.md。
- ⚠️ **本輪踩到 class 撞名**：`session.js:482` 的練球主頁本來就叫 `.page--home`，
  新首頁初版同名 → landing 的 `color:#fff` 汙染練球頁（淺色模式白字）。已改名
  `.page--landing`。**新頁面的外層 class 命名前先 grep 一次**。

## 驗收方法論（Fable 必跑，血淚換來的）

1. **分享卡垂直帶掃描**：2/4 球種 × 紙感/照片 4 張，逐 y 掃墨水帶，
   相鄰帶間 ≥16px（M4.3 前漏檢造成實機重疊，使用者抓到）。
   左右兩欄佈局要**分欄掃**，全寬掃會誤報。
2. 對齊掃描：跨行顆數右緣、% 右緣像素級切齊。
3. 本機 QA 快取三層陷阱：dev server 掛掉時 **SW 離線快取會餵舊檔**
   （network-first 斷網 fallback，看起來像沒改到）；瀏覽器對 js 模組有
   **HTTP 啟發式快取**（python http.server 只給 Last-Modified）。
   驗收前固定：unregister SW → caches 全清 → 逐檔 fetch {cache:'reload'}
   → reload。
4. `hidden` 屬性會被任何 display class 蓋掉——app.css 已加
   `[hidden]{display:none!important}` 根治，新元件別再踩。

## 開發分工（使用者指定，已入長期記憶）

Fable 讀碼查根因寫 spec（docs/SPEC_*.md）→ Sonnet subagent 實作
（明令不 commit 不 push）→ Fable 瀏覽器實測驗收（含上述掃描）→
Fable commit＋push（自動部署）。

## 鐵律（不變）

- 純 vanilla JS ES modules、無 build step、不引入套件、不做 CV/帳號雲端。
- js/menus.js 球員數據與菜單出處雙來源查證，不得憑印象改。
- UI 文案繁體中文；深色/淺色兩套都要驗；觸控目標 ≥44px。
- 改版遞增 sw.js 的 CACHE_NAME。

## 待辦 / 觀察

- **無描邊熱區字**的實機觀感等使用者回饋——若貫穿處壓線顯糊，
  備案是把球場線在字的位置斷開（不回加描邊）。
- 兩台機器 OneDrive＋git 同步照舊；此機 git 身分 repo-local。
- 後續里程碑（M2 統計深化的殘項、雙語）看使用者實地使用節奏再排。
