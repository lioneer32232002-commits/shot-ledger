# HANDOFF 2026-07-16：三星制（第三包）續作交接

> ✅ **本包已於 2026-07-17 完成並上線**（schema 10、SW v24），總結見 docs/SPEC_M7.md。
> 以下為當時的交接內容，僅留檔。

> 給接手的 session／機器：本文件是唯一入口。分工慣例＝Fable 寫 spec →
> Sonnet subagent 實作 → Fable 驗收（見記憶 fable-spec-sonnet-implement-workflow）。
> 部署全自動：push main → GitHub Actions → Cloudflare Pages（shot-ledger.pages.dev）。
> QA 注意本機模組快取三層陷阱（見 docs/HANDOFF_20260714.md 的驗收方法論）。

## 一、現況（2026-07-16 深夜暫停時）

- **main = d31f9e7，已部署，乾淨可用**（SW cache v23）。
- 今日已上線三個 commit：
  - `d09a3f7` UX 走查修正四項（SW v21）
  - `7cda3c1` 誠實機制 2.0＋挑戰菜單刪簡易版＋PB 標球數（SW v22，schema 8）
  - `d31f9e7` 第 11 關 Lin 台灣時期＋layup 球種＋第 1 關新手化，階梯 13 關（SW v23，schema 9）
- **本分支（wip/three-stars）帶著第三包的半成品**：
  - `js/stats.js`：**已完成**——`groupTotals()`、`evaluateSignature(menuId, rounds)`（13 關 switch 全表）、`export evaluateStars(menu, session)`。未經驗收。
  - `js/store.js`：**做到一半**——applyChallengeResult 的 stars OR 合併動了一部分；**schema 10 migration 尚未寫**（SCHEMA_VERSION 仍是 9）。
  - 顯示層完全未動。
- 暫停原因：使用者額度用滿，主動喊停（Sonnet agent 已 kill，喚醒排程已清）。

## 二、第三包剩餘工作（spec 原文重點）

概念：每關三顆星，只有 ★1 影響解鎖（現有邏輯零改動）。星星只由
「isChallengeEligible＋完整版」場次計入，一經取得永不失去（OR 合併）。

- ★1 解鎖星 = evaluatePassRule pass（現有門檻）
- ★2 簽名星 = 每關專屬規則（judgment 已在 stats.js，13 關 label/desc 需補進 menus.js 各關 `signature: { label, desc }`）
- ★3 高標星 = passRule 每條 minPct **+10pp** 全數達成

### 剩餘待辦清單

1. **store.js 完成**：
   - `progress.stars = { [menuId]: { unlock, signature, high } }`
   - applyChallengeResult：eligible 時 evaluateStars → OR 進 stars，回傳加 `newStars`（本次新增的星）
   - confirmPace 列入時同路徑自動生效（已共用 applyChallengeResult，應自動成立，驗證即可）
   - SCHEMA_VERSION 9→10：v10 掃全部歷史場次（challenge＋full＋endedAt＋eligible）回溯發星
2. **menus.js**：13 關各加 `signature: { label, desc }`（全表見下）
3. **顯示層**（session.js＋app.js＋css/app.css）：
   - 階梯磁磚（刊號卡）：關卡名下方三顆小星（~10px），拿到＝實心（accent；已通過關用 success）、未拿到＝border 色輪廓；**鎖定磁磚不顯示星**
   - 變體 sheet：門檻區改三行「★ 過關門檻：{passDesc}」「★★ {label}：{desc}」「★★★ 高標：{+10pp 描述}」
   - 結算頁挑戰結果：三星狀態列＋「新獲得 ★★ 簽名星：課表收官」（challenge-note--record 語言）
   - Hero card：關卡編號旁顯示星數（乾淨為準）
   - 設定頁資料狀態：「星星：X / 39」
4. **驗收**：解鎖邏輯零改動；schema 9 舊資料含歷史達標場次 → v10 後 stars 回溯出現；舊 12 輪 lin_college 場次的簽名星判定不炸；種 3-4 關資料實測簽名星；console 無錯誤
5. **收尾**：SW 快取 v23→**v24**、commit、push main（自動部署）、補寫 docs/SPEC_M7.md 總結（誠實機制 2.0／台灣時期關／三星制三包）

### 13 關簽名星全表（stats.js 已實作，menus.js 顯示文案用）

| menu.id | label | desc |
|---|---|---|
| lin_college | 課表收官 | 最後一輪罰球投 7 進以上 |
| lin_dleague | 折返穩定 | 左右兩翼命中率差 ≤10 個百分點 |
| lin | 先發制人 | 前 3 輪就打出過關命中率 |
| dirk_rookie | 單點鑽研 | 罰球線頂沒有一輪失手過半 |
| dirk | 金雞獨立 | 單輪 8 進的手感時刻 |
| allen_bucks | 底角殺手 | 兩側底角合計 ≥40% |
| allen | 儀式不亂 | 沒有任何一輪掉到 3 球以下 |
| klay_rise | 穩了才換點 | 每個連兩輪的點位，第二輪不退步 |
| klay | 量產不停 | 下半場總進球不輸上半場 |
| lillard | Dame Time | 最後一個深三輪投 4 進以上 |
| lin_taiwan | 左右開弓 | 左右上籃各 ≥70% |
| curry_mvp | 全點開花 | 每個三分點位都至少 4 進 |
| curry | 雙修 | 3 分與深 3 同場皆超門檻 5 個百分點 |

判定細節（邊界條件）以 stats.js `evaluateSignature()` 現有實作為準——它是照
spec 全表寫的，接手先讀一遍再驗，不要重寫。

## 三、今天的關鍵設計決定（避免接手誤改）

- **誠實機制 2.0**：唯一標準＝輪距中位。≥60s 自動列入／30–60s 結算詢問卡
  （回答寫 `session.paceConfirmed`，只問一次）／<30s 不列入。總時長門檻已廢。
- **簡易版**：挑戰菜單已刪（綜合巡迴保留）。舊 easy 場次紀錄照常顯示。
- **lin_taiwan 數據**：2024-25 TPBL 例行賽，雙來源（TPBL 官網球員頁＋2024-25
  國王賽季維基）。**2024 PLG FMVP 是李愷諺不是林書豪**——文案不得寫錯（menus.js 頭部有警語）。
- **migration 原則**：只加不減（解鎖、徽章永不收回）。v8 用新誠實規則重算、
  v9 補 lin_taiwan、v10（待寫）回溯發星。
- **PB date**：applyChallengeResult 用 `session.endedAt`（不要改回 new Date()——
  confirmPace 補確認舊場次時日期會錯）。

## 四、待辦（第三包之後）

- ~~分享卡 5 球種極端組合（free 場次同時有 2分/3分/深3/罰球/上籃＋2 徽章＋照片模式）
  未做垂直帶掃描——抽查一張。~~ ✅ 2026-07-17 掃描通過（見 SPEC_M7.md）。
- 使用者先前發過的獨立背景任務「修正 active header 未閉合的 variant-tag span」
  ——主 session 全庫 grep 過**查無此筆誤**，多半是誤報，接手可忽略或關掉。
