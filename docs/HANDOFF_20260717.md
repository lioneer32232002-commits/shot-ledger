# HANDOFF 2026-07-17：M7 三包全部上線，待辦清空

> 給接手的 session／機器：本文件是唯一入口。分工慣例＝Fable 讀碼寫 spec →
> Sonnet subagent 實作（不 commit 不 push）→ Fable 瀏覽器實測驗收 →
> Fable commit＋push（自動部署）。驗收方法論見 docs/HANDOFF_20260714.md。

## 一、現況（2026-07-17）

- **main = `64e3b4d`，工作樹乾淨，與 origin 同步，無殘留分支**
  （wip/three-stars 已 squash 併入後刪除；另兩個已合併的 claude/* 舊分支
  與另一台機器的 prunable worktree 也已清理）。
- **線上＝SW v24、schema 10，已用線上 sw.js 版號實際驗證**。
- M7 三包（誠實機制 2.0／第 11 關 Lin 台灣時期／三星制）全部上線，
  總結與驗收紀錄見 **docs/SPEC_M7.md**。
- 前次交接（HANDOFF_20260716）的待辦已全部清空：
  - 三星制第三包 ✅ 完成上線（三星判定、store 發星＋v10 回溯、全顯示層、SW v24）
  - 分享卡 5 球種極端組合垂直帶掃描 ✅ 通過（SPEC_M7 有完整間距表）
  - variant-tag span 筆誤 ✅ 前次已查證為誤報，關閉

## 二、本日重大教訓（部署卡關，鐵律已入記憶與 SPEC_M7）

**CI 測試會擋部署**：Deploy workflow 先跑 `node tools/test_stats.mjs`，失敗就不部署。
v22、v23 兩包當時被一條誠實機制 1.0 的過時時長測試連續擋下，**線上一度停在
v21**，而前次交接誤寫「已部署」。`6ad7f75` 已把測試更新為 2.0 規則＋補三星制
測試（88 條）。此後：

1. 改 `js/stats.js` 行為，同一個 commit 必須同步 `tools/test_stats.mjs`（本地先跑）。
2. push main 後跑 `gh run watch` 確認 workflow 綠燈。
3. 最終以線上為準：抓 `https://shot-ledger.pages.dev/sw.js` 驗 `CACHE_NAME`。
4. 交接文件的「已部署」聲明一律用第 3 點重新驗證。

## 三、現行機制速查（詳見 SPEC_M7.md）

- **誠實機制 2.0**：唯一標準＝輪距中位。≥60s auto／30–60s 問一次
  （`session.paceConfirmed`）／<30s 不列入。總時長門檻已廢。
- **三星制**：★1 解鎖（=passRule，解鎖邏輯零改動）／★2 簽名（每關專屬，
  `stats.evaluateSignature`）／★3 高標（passRule 每條 +10pp）。只有
  eligible＋完整版計入，OR 合併永不收回；v10 migration 已回溯發星。
- **階梯 13 關**（tier 11 = lin_taiwan，上籃球種）。
  ⚠️ 2024 PLG FMVP 是李愷諺不是林書豪，文案不得寫錯（menus.js 有警語）。
- **PB date** 用 `session.endedAt`，不可改回 `new Date()`。
- migration 原則：只加不減（解鎖、徽章、星星永不收回）。

## 四、待辦

（空——沒有已知的未完成工作。下一步方向由使用者決定。）
