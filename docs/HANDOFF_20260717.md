# HANDOFF 2026-07-17：M7 三包＋M8 五包全部上線，待辦清空

> 給接手的 session／機器：本文件是唯一入口。分工慣例＝Fable 讀碼寫 spec →
> Sonnet subagent 實作（不 commit 不 push）→ Fable 瀏覽器實測驗收 →
> Fable commit＋push（自動部署）。驗收方法論見 docs/HANDOFF_20260714.md。
> UI 改版另有慣例：先出 2〜3 案比稿（mockup 吃真實 tokens/app.css）給使用者選。

## 一、現況（2026-07-17 收工）

- **main = `d539c12`，工作樹乾淨，與 origin 同步，無殘留分支／worktree**。
- **線上＝SW v29、schema 11，已用線上 sw.js／badges.js 實際驗證**。
- 上午：M7 三包（誠實機制 2.0／第 11 關 Lin 台灣時期／三星制）→ **docs/SPEC_M7.md**。
- 下午：M8 五包（變體標籤極簡化／變體面板排版統一／徽章牆／徽章搬統計頁＋
  練球頁成就條／徽章擴充 17 顆＋schema 11 回溯發章）→ **docs/SPEC_M8.md**。

## 二、部署鐵律（v22/v23 曾被過時測試連續擋下，教訓詳見 SPEC_M7）

1. 改 `js/stats.js` 行為，同一個 commit 必須同步 `tools/test_stats.mjs`（本地先跑，現 96 條）。
2. push main 後跑 `gh run watch` 確認 workflow 綠燈（CI 測試失敗＝不部署）。
3. 最終以線上為準：抓 `https://shot-ledger.pages.dev/sw.js` 驗 `CACHE_NAME`。
4. 交接文件的「已部署」聲明一律用第 3 點重新驗證。
5. 兩台機器共推 main：改 schema／SW 版號前必先 `git fetch`，會撞號。

## 三、現行機制速查

- **誠實機制 2.0**（SPEC_M7）：唯一標準＝輪距中位。≥60s auto／30–60s 問一次
  （`session.paceConfirmed`）／<30s 不列入。總時長門檻已廢。
- **三星制**（SPEC_M7）：★1 解鎖（=passRule）／★2 簽名（`stats.evaluateSignature`）
  ／★3 高標（+10pp）。只有 eligible＋完整版計入，OR 合併永不收回。
- **徽章 17 顆**（SPEC_M8）：出席 5／投量 6／摘星 3／階梯 3，四家族。
  顯示層集中 `js/badges.js`：統計頁徽章牆＋練球頁「下一顆徽章」成就條，
  設定頁已無徽章。發章在 `store.applyChallengeResult` 尾端，v11 migration
  已回溯（出席用歷史最長連續 `maxStreakDays`）。
- **變體**：「完整」是預設不標，只標「簡易」；挑戰面板＝置中 hero＋
  「三星目標／菜單依據／誠實機制」卡片系統，單一選項滿版。
- **階梯 13 關**（tier 11 = lin_taiwan，上籃球種）。
  ⚠️ 2024 PLG FMVP 是李愷諺不是林書豪，文案不得寫錯（menus.js 有警語）。
- **PB date** 用 `session.endedAt`，不可改回 `new Date()`。
- migration 原則：只加不減（解鎖、徽章、星星永不收回）。

## 四、待辦

（空——沒有已知的未完成工作。下一步方向由使用者決定。）
