# HANDOFF 2026-07-20：M9 資料安心包上線，待辦清空

> 給接手的 session／機器：本文件是唯一入口。分工慣例＝Fable 讀碼寫 spec →
> Sonnet subagent 實作（不 commit 不 push）→ Fable 瀏覽器實測驗收 →
> Fable commit＋push（自動部署）。驗收方法論見 docs/HANDOFF_20260714.md。
> UI 改版另有慣例：先出 2〜3 案比稿（mockup 吃真實 tokens/app.css）給使用者選。

## 一、現況（2026-07-20 收工）

- **main = `3508737`，工作樹乾淨，與 origin 同步**。
- **線上＝SW v30、schema 11，已抓線上 sw.js 驗證 CACHE_NAME**。
- 本日：M9 資料安心包（規格＝**docs/SPEC_M9.md**，比稿 C 案）：
  - 設定頁「備份與轉移」加安心主軸說明段（紀錄存本機、平常不用手動存檔），
    移除紅底催備份提醒。
  - 啟動時 best-effort `navigator.storage.persist()`，成功才在設定頁顯示
    綠點狀態列（失敗態不顯示，避免焦慮）。
  - 結束頁里程碑備份小卡：未備份 ≥30 次才出現；「先不用」snooze 後要再
    累積 30 次；「匯出」原地變完成態。`settings.backupNudgeBase` 走保底
    補欄位，**沒動 schema 版號**。
  - 明確決策：**不做 D1 同步碼雲端備份**（無跨裝置需求、使用者少）；
    要加隨時可疊上，不衝突。
- 昨日另一台機器上線：全期熱區改版＋圓形數字鈕修正（`00ad376`、`db96572`），
  當時**沒 bump SW 版號**，由本次 v30 一併送達既有使用者。

## 二、部署鐵律（不變）

1. 改 `js/stats.js` 行為，同 commit 必須同步 `tools/test_stats.mjs`（現 96 條）。
2. push main 後 `gh run watch` 綠燈（CI 失敗＝不部署；本日排隊 20 分鐘才跑完，
   耐心等）。
3. 最終以線上為準：抓 `https://shot-ledger.pages.dev/sw.js` 驗 `CACHE_NAME`。
4. 兩台機器共推 main：改 schema／SW 版號前必先 `git fetch`（本日又撞到一次：
   fetch 才發現落後 2 commits）。
5. 本機驗證陷阱：hash 導航不會重載模組，驗證前要真正 `location.reload()`；
   必要時對改過的檔案 `fetch(url, {cache:'reload'})` 暖快取。

## 三、現行機制速查（M7／M8 不變，補 M9）

- **誠實機制 2.0**（SPEC_M7）：唯一標準＝輪距中位。≥60s auto／30–60s 問一次／<30s 不列入。
- **三星制**（SPEC_M7）：★1 解鎖／★2 簽名／★3 高標。OR 合併永不收回。
- **徽章 17 顆**（SPEC_M8）：顯示層集中 `js/badges.js`；發章在
  `store.applyChallengeResult` 尾端。
- **備份提醒**（SPEC_M9）：只在練球結束頁（`opts.onDone` 存在）、
  `unbackedUpCount ≥ 30` 且未 snooze 時出卡；匯出（JSON）會重設
  `backupNudgeBase`。歷史詳情永不出卡。
- **階梯 13 關**（tier 11 = lin_taiwan）。⚠️ 2024 PLG FMVP 是李愷諺不是林書豪。
- **PB date** 用 `session.endedAt`；migration 原則只加不減。

## 四、待辦

（空——沒有已知的未完成工作。下一步方向由使用者決定。）
