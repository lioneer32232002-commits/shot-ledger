# HANDOFF 2026-07-21：M10／M11／M12 三包上線，待辦清空

> 給接手的 session／機器：本文件是唯一入口。分工慣例＝Fable 讀碼寫 spec →
> Sonnet subagent 實作（不 commit 不 push）→ Fable 瀏覽器實測驗收 →
> Fable commit＋push（自動部署）。驗收方法論見 docs/HANDOFF_20260714.md。
> UI 改版另有慣例：先出 2〜3 案比稿（mockup 吃真實 tokens/app.css）給使用者選。

## 一、現況（2026-07-21 收工）

- **main = `a2b16cb`（本文件 commit 後為其後一筆），工作樹乾淨，與 origin 同步**。
- **線上＝SW v33、schema 12，已抓線上 sw.js／menus.js／badges.js 驗證**。
- 本日三包（規格＝ `docs/SPEC_M10.md`／`SPEC_M11.md`／`SPEC_M12.md`）：
  1. **M10 生涯分享卡＋疲勞趨勢改版**：統計頁生涯累計加分享鈕，產出 1080×1350
     生涯卡（生涯數字／階梯進度條／徽章列，沿用紙感＋5 張底圖＋自訂照片那套
     sheet，已抽成 `openCardSheet` 共用）。疲勞趨勢下段「逐球前後段」換成
     **單場逐輪**（挑戰場次可左右切換、0 出手輪折線斷開、前後半輪比較）——
     逐球輸入不再是前提（使用者實際上不用逐球）。
  2. **M11 挑戰階梯 13 → 15 關**：新增 tier 11 `brunson`（2026 FMVP，中距 9 輪
     ＋罰球 4＋上籃 2）與 tier 14 `bird`（1984/1986 FMVP，三分 7＋中距 4＋罰球 4），
     **新關一律 15 輪 150 球**，既有 13 關的數據與輪次序列完全不動、只重編 tier。
  3. **M12 徽章改刊號數字盤**：17 顆改成「門檻數字當主角」（`3天`／`1K`／`45★`／
     `15關`＋家族小標記），解決「分享卡上徽章沒有說明、同家族長一樣」的根因。
- **順手修掉的既有缺陷**：「通過」原本是推導的（下一關已解鎖＝這關通過），
  導致插入中段的新關會被玩家沒打過就自動判定成已通過（第 11 關 `lin_taiwan`
  當初插入時就已中招）。改為 `progress.passed` 明確記錄，schema 12 用**改版前的
  13 關順序**（`LADDER_PRE_M11`）回推，新關只補 `unlocked` 不補 `passed`。
- **新增工具與流程**：`docs/ADDING_A_TIER.md`（加關卡 SOP）＋
  `tools/check_ladder.mjs`（階梯一致性檢查）。本日實戰驗證有效，抓到兩個沒人
  發現的洞：`index.html` 漏改的關數文案、上述通過狀態的判定漏洞。

## 二、部署鐵律（不變）

1. 改 `js/stats.js` 行為，同 commit 必須同步 `tools/test_stats.mjs`（現 **104** 條）。
2. **動階梯／菜單／徽章，收工前 `node tools/check_ladder.mjs` 必須 0 FAIL**。
3. push main 後 `gh run watch` 綠燈（CI 失敗＝不部署）。
4. 最終以線上為準：抓 `https://shot-ledger.pages.dev/sw.js` 驗 `CACHE_NAME`
   （CDN 有短暫快取，驗不到就加 `?cb=$(date +%s)` 再抓一次）。
5. 兩台機器共推 main：改 schema／SW 版號前必先 `git fetch`。
6. 本機驗證陷阱：hash 導航不會重載模組，驗證前要真正 `location.reload()`；
   模組要加 `?v=Date.now()` 破快取。**本環境的瀏覽器截圖工具常逾時**——改用
   canvas `toDataURL` 下載成 PNG 再讀圖、或用 DOM 量測／像素掃描自驗。

## 三、現行機制速查

- **挑戰階梯 15 關**：tier 11 = `brunson`、tier 14 = `bird`，末關仍是 `curry`
  終極試煉（全破徽章語意不變）。新關 15 輪；既有關維持 12 輪（`lin_taiwan` 13 輪）。
- **通過狀態**：`progress.passed` 明確記錄，唯一寫入路徑是
  `store.applyChallengeResult()`。顯示層（`badges.ladderProgress`／
  `sharecard.buildLadderCells`／`session.currentLadderState`）全部讀它。
  ⚠️ **絕不可再回退成「下一關已解鎖」推導**，`check_ladder` 第 9 項會抓。
- **誠實機制 2.0**（SPEC_M7）：唯一標準＝輪距中位。≥60s auto／30–60s 問一次／<30s 不列入。
- **三星制**（SPEC_M7）：★1 解鎖／★2 簽名／★3 高標（門檻 +10pp）。OR 合併永不收回。
  新關的簽名規則：`brunson`＝最後 3 輪合計 ≥60%；`bird`＝同場 50-40-90。
- **徽章 17 顆**（SPEC_M12）：數字盤顯示層集中 `js/badges.js`（`badgeNumeral()`），
  發章仍在 `store.applyChallengeResult` 尾端。`45★`（摘星全滿）與 `15關`（全破）
  **依 `ladderMenus()` 動態算，寫死會被 check_ladder 擋下**。
  單位只在分享卡畫（App 內 44/68px 旁邊有全名，5–8px 中文字不可讀）。
- **生涯分享卡**：入口在統計頁生涯累計卡；版面採「先量測再把剩餘空間分配到各段
  間隙」，徽章 ≤7 顆一列／8–14 兩列／15+ 三列（>21 才出現 ＋N）；照片模式的
  徽章盤有半透明深色晶片底（實測對比 6.1–8.1，通過 WCAG AA）。
- **PB date** 用 `session.endedAt`；migration 原則只加不減。

## 四、資料查證紀律（再次強調）

球員數據與菜單出處**一律雙來源交叉核對**，查證紀錄寫進 `js/menus.js` 檔頭。
本日新增兩關的來源見 `docs/SPEC_M11.md` §0。

⚠️ 已澄清的事實：**Allen Iverson 沒有拿過 FMVP、也沒拿過總冠軍**（2001 年是
例行賽 MVP，該年 FMVP 是 Shaquille O'Neal），使用者曾提議加他，已說明不列入。
2024 PLG FMVP 是李愷諺不是林書豪（舊教訓）。

## 五、待辦

- 使用者目前實際進度在 **Lin 系列**，第 11／14 關還沒機會實地打過——新關的難度
  校準（15 輪 150 球會不會太長、門檻會不會太硬）等使用者實測回饋再調。
- 其餘無已知未完成工作。下一步方向由使用者決定；要再加關就照
  `docs/ADDING_A_TIER.md` 走。
